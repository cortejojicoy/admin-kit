<!-- Generated by `admin-kit docs`. Edits will be overwritten. -->

# Resources

1 declared. Each is a set of endpoints plus the fields that drive the generated screens.

## People

Resource name: `users` · id field: `id` · page size: 25

### Endpoints

| Operation | Method | Path | Permission |
| --- | --- | --- | --- |
| `list` | `GET` | `/api/users` | `users:list` |
| `one` | `GET` | `/api/users/:id` | `users:read` |
| `create` | `POST` | `/api/users` | `users:create` |
| `update` | `PATCH` | `/api/users/:id` | `users:update` |
| `remove` | `DELETE` | `/api/users/:id` | `users:delete` |

### Fields

| Field | Type | Required | In list | In form |
| --- | --- | --- | --- | --- |
| `id` | string | — | ✓ | — |
| `name` | string | yes | ✓ | ✓ |
| `email` | email | yes | ✓ | ✓ |
| `role` | select | — | ✓ | ✓ |
| `active` | boolean | — | ✓ | ✓ |

### Screens

```tsx
import { ResourceTable, ResourceForm, ResourceShow } from '@cortejojicoy/admin-kit/ui'

<ResourceTable resource="users" hrefFor="/users/:id" />
<ResourceForm resource="users" />
<ResourceShow resource="users" id={id} />
```

### Hooks

```ts
import { useList, useOne, useCreate, useUpdate, useDelete } from '@cortejojicoy/admin-kit/data'

const { rows, total, loading, page, setPage } = useList('users')
const { data } = useOne('users', id)
const create = useCreate('users')
```
